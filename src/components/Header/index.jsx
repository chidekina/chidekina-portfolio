import NavMenuHeader from "../NavMenuHeader";

const Header = () => {
    return (
        <header className="bg-secondary px-12 py-4 text-white">
            <NavMenuHeader />
        </header>
    );
}

export default Header;